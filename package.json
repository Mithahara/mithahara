{
  "name": "mithahara",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build --outDir docs",
    "preview": "vite preview --host",
    "serve": "python3 -m http.server 3000"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}